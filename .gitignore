# Clones de repos de terceiros p/ estudo (nunca subir — ver planos/13)
referencias/
# Projeto de terceiro deixado dentro da árvore; fora do monorepo. Sem esta linha o
# Biome (que respeita o .gitignore) o varre e o gate acusa ~16k erros que não são nossos.
odysseus/

# Anexos locais do Hermes (arquivos que o usuário arrasta para o chat)
.hermes/

# Futuro código
node_modules/
dist/
dist-site/
coverage/
*.tsbuildinfo

# Relatórios e caches locais de ferramentas
.vitest/
.cache/

# Dados locais do CodingPro (checkpoints/undo, sessões) — nunca versionar
.codingpro/

# Segredos — NUNCA commitar (chaves ficam fora do repo; ver README)
.env
.env.*
*.local.json
credentials*.json
release/
packages/desktop/.pack/
